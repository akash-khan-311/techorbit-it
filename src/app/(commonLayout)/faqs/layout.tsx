export const metadata = {
  title: "FAQs — TechOrbit IT",
  description: "Learn about TechOrbit IT's frequently asked questions.",
  alternates: {
    canonical: "https://techorbitit.com/faqs",
  },
};
const FAQLayout = ({ children }: { children: React.ReactNode }) => {
  return <>{children}</>;
};

export default FAQLayout;
