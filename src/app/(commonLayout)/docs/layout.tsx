export const metadata = {
  title: "Docs — TechOrbit IT",
  description: "Documentation and resources for TechOrbit IT.",
  alternates: {
    canonical: "https://techorbitit.com/docs",
  },
};

const layout = ({ children }: { children: React.ReactNode }) => {
  return <>{children}</>;
};

export default layout;
