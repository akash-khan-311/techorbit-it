export const metadata = {
  title: "Community — TechOrbit IT",
  description: "Learn about TechOrbit IT's community and engagement programs.",
  alternates: {
    canonical: "https://techorbitit.com/community",
  },
};

const CommunityLayout = ({ children }: { children: React.ReactNode }) => {
  return <>{children}</>;
};

export default CommunityLayout;
